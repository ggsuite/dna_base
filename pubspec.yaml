name: base_dna
description: The base DNA of our projects - shared configs, docs, scripts and agent skills inherited by every repository via gg_dna.
version: 1.1.1
repository: https://github.com/ggsuite/base_dna.git

environment:
  sdk: ">=3.11.0 <4.0.0"

dev_dependencies:
  gg_dna: ^4.1.1
  lints: ^6.1.0
  test: ^1.31.2